spring:
  application:
    ## 配置应用名称
    name: msc-payment
  cloud:
    nacos:
      config:
        ## 配置中心地址
        server-addr: 127.0.0.1:8848
        ## 配置文件扩展名称
        file-extension: yml

  profiles:
    active: qa
